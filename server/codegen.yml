overwrite: true
schema: "http://localhost:4000"
#documents: null
generates:
  ./src/graphql/resolvers.types.ts:
    config:
      useIndexSignature: true
    plugins:
      - "typescript"
      - "typescript-resolvers"
